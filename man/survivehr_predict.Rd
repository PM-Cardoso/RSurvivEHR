% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{survivehr_predict}
\alias{survivehr_predict}
\title{Predict cumulative incidence with a fine-tuned RSurvivEHR model}
\usage{
survivehr_predict(
  model_bundle,
  events,
  static_covariates = NULL,
  max_new_tokens = 1L,
  eval_times = NULL
)
}
\arguments{
\item{model_bundle}{A model bundle returned by \code{survivehr_finetune()} or
\code{survivehr_load_model()}.}

\item{events}{data.frame with columns \code{patient_id}, \code{event}, \code{age},
optional \code{value}.  Should \strong{not} contain the outcome event (leakage-free).}

\item{static_covariates}{optional data.frame with \code{patient_id} and the
same covariate columns used at training time.}

\item{max_new_tokens}{number of autoregressive steps (pretrain models only;
ignored for fine-tuned models).}

\item{eval_times}{An optional numeric vector of time points (in the same
units as \code{age}) at which to read the cumulative-incidence CDF for
fine-tuned models.  Each value must be in \verb{(0, outcome_horizon]} — the
ODE prediction window stored in the fine-tuned bundle.  For example,
with \code{outcome_horizon = 5.0} (years) use \code{eval_times = c(1, 2, 3, 5)}
to obtain 1-, 2-, 3- and 5-year risks.
When \code{NULL} (default) only \verb{_cdf_last} (risk at the full horizon) and
\verb{_auc} (average risk) are returned, preserving backward compatibility.
Ignored for pretrain models.}
}
\value{
For a \strong{fine-tuned} model, a \code{data.frame} with columns:
\describe{
\item{\code{patient_id}}{Patient identifier.}
\item{\verb{\{outcome\}_cdf_last}}{Cumulative incidence at the \strong{end} of the
prediction window (\code{t = outcome_horizon}).}
\item{\verb{\{outcome\}_auc}}{Area under the CDF integrated from 0 to
\code{time_scale}.  Interpretable as average risk over the window.}
\item{\verb{\{outcome\}_cdf_t\{X\}}}{\emph{(Only when \code{eval_times} is supplied.)}
Cumulative incidence at time \code{X} (same units as \code{age}).  One column
per requested time point, e.g. \code{CVD_cdf_t1}, \code{CVD_cdf_t2.5}.}
}
For a \strong{pretrain} model, a \code{data.frame} with one row per generated step
per patient:
\describe{
\item{\code{patient_id}}{Patient identifier.}
\item{\code{step}}{Generation step (1 = next event, 2 = event after that, …).}
\item{\code{generated_token}}{Vocabulary token ID of the generated event.}
\item{\code{generated_event}}{Decoded event name (e.g. \code{"HYPERTENSION"}).}
\item{\code{generated_age}}{Predicted age of the generated event in the same
units as \code{age} (de-normalised by \code{time_scale}).}
\item{\code{generated_value}}{Predicted numeric value for that event (e.g.
a lab result) in the original input units (automatically
de-standardised per event); \code{NaN} for non-measurement events.}
}
}
\description{
Runs forward inference on new event sequences using a fine-tuned model
bundle.  The prediction window length and age normalisation divisor
(\code{time_scale}) are read automatically from the bundle — no need to
supply them at inference time.
}
\details{
The \code{events} frame should have the outcome event codes removed (same
as the context filtering applied at fine-tune time) to ensure
leakage-free predictions.
}
\examples{
\dontrun{
# ---- Prediction using ft / events_pop / static_pop from examples above -----
# Remove the outcome event from prediction context (leakage-free)
pred_events <- events_pop[events_pop$event != "CVD", ]

preds <- survivehr_predict(ft, pred_events, static_pop)
print(preds)
# Columns: patient_id, CVD_cdf_last, CVD_auc
#
# CVD_cdf_last : probability of CVD within outcome_horizon (5 years when
#                outcome_horizon = 5.0); stored in the bundle automatically
# CVD_auc      : average cumulative CVD risk over that window;
#                higher = greater overall risk
}
}
